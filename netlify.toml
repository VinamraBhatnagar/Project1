# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"

# Functions (optional — if you add Netlify Functions later)
[functions]
  directory = "netlify/functions"

# Redirect all requests to index.html (for React Router / SPA support)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
